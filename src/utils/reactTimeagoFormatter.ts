import engStrings from 'react-timeago/lib/language-strings/en';
import buildFormatter from 'react-timeago/lib/formatters/buildFormatter';

export const defaultTimeagoFormatter = buildFormatter(engStrings);
